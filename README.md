Steps to run the video_step_analyzer.py script:

Save the code: Copy the Python code from the previous markdown cell and save it as a file named video_step_analyzer.py.

Create sample_data directory: In the same directory where you saved video_step_analyzer.py, create a new folder named sample_data.

Place videos: Put your student.mp4 and chef.mp4 (or whatever your input video is if you modify the trimming logic) files into the sample_data directory.

Open a terminal/command prompt: Navigate to the directory where you saved video_step_analyzer.py.

Run the script: Execute the script using the command: **python video_step_analyzer.py**
